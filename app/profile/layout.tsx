export const metadata = {
  title: "Profile | BRM CSGO",
  description: "CS GO Skins Marketplace",
};

export default async function ProfileLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
